apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-crashloop
  namespace: devops-homework
spec:
  restartPolicy: Always
  containers:
  - name: app
    image: busybox:1.37
    command:
    - sh
    - -c
    - echo intentional-crash; exit 1
