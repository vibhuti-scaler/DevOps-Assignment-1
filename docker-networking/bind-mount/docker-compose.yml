# Compose equivalent of the bind-mount run.
# The source path is relative to this file, so the same mount works anywhere
# the repository is cloned.
services:
  web:
    image: nginx:1.27-alpine
    container_name: bind-mount-web
    ports:
      - "8082:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
