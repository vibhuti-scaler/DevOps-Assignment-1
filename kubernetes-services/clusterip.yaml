apiVersion: v1
kind: Service
metadata:
  name: web-clusterip
  namespace: devops-homework
spec:
  type: ClusterIP
  selector:
    app: service-web
  ports:
  - name: http
    port: 80
    targetPort: http
