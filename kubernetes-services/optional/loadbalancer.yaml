apiVersion: v1
kind: Service
metadata:
  name: web-loadbalancer
  namespace: devops-homework
spec:
  type: LoadBalancer
  selector:
    app: service-web
  ports:
  - name: http
    port: 80
    targetPort: http
